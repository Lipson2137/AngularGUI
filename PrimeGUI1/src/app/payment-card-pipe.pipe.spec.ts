import { PaymentCardPipePipe } from './payment-card-pipe.pipe';

describe('PaymentCardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentCardPipePipe();
    expect(pipe).toBeTruthy();
  });
});
