import { ProductKeyPipesPipe } from './product-key-pipes.pipe';

describe('ProductKeyPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductKeyPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
