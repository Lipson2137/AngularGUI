import { PaymentCardBlurPipePipe } from './payment-card-blur-pipe.pipe';

describe('PaymentCardBlurPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentCardBlurPipePipe();
    expect(pipe).toBeTruthy();
  });
});
