import { ReleaseCalendarPipePipe } from './release-calendar-pipe.pipe';

describe('ReleaseCalendarPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseCalendarPipePipe();
    expect(pipe).toBeTruthy();
  });
});
